<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <!-- Type spinner — selalu tampil -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/type" />

        <Spinner
            style="@style/ComboBox"
            android:id="@+id/SType"
            android:layout_width="match_parent"
            android:spinnerMode="dialog" />

        <!-- Shape — gone by default, tampil untuk BUTTON, TOUCHSCREEN_TOGGLE,
             D_PAD, MENU_NAVIGATION, MULTIPLE_BUTTON -->
        <LinearLayout
            android:id="@+id/LLShape"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/shape" />

            <Spinner
                style="@style/ComboBox"
                android:id="@+id/SShape"
                android:layout_width="match_parent"
                android:spinnerMode="dialog" />
        </LinearLayout>

        <!-- Cursor Move Mode — hanya tampil saat type = RIGHT_STICK -->
        <LinearLayout
            android:id="@+id/LLCursorMove"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <CheckBox
                android:id="@+id/CBCursorMove"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Cursor Move Mode" />

        </LinearLayout>

        <!-- Cursor Move Speed — hanya tampil saat CBCursorMove dicentang -->
        <LinearLayout
            android:id="@+id/LLCursorMoveRadius"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="4dp"
            android:visibility="gone">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Cursor Speed" />

                <TextView
                    android:id="@+id/TVCursorMoveRadius"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:minWidth="40dp"
                    android:gravity="end"
                    android:text="30%" />

            </LinearLayout>

            <SeekBar
                android:id="@+id/SBCursorMoveRadius"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:max="100"
                android:progress="30" />

        </LinearLayout>

        <!-- Scale — selalu tampil -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/scale"/>

            <TextView
                android:id="@+id/TVScale"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <SeekBar
            android:id="@+id/SBScale"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:min="50"
            android:max="150" />

        <!-- Range options — gone by default, hanya untuk RANGE_BUTTON -->
        <LinearLayout
            android:id="@+id/LLRangeOptions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/range" />

            <Spinner
                style="@style/ComboBox"
                android:id="@+id/SRange"
                android:layout_width="match_parent"
                android:spinnerMode="dialog" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/orientation" />

            <RadioGroup
                android:id="@+id/RGOrientation"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <RadioButton
                    android:id="@+id/RBHorizontal"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/horizontal" />

                <RadioButton
                    android:id="@+id/RBVertical"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/vertical" />
            </RadioGroup>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/columns" />

            <com.winlator.cmod.widget.NumberPicker
                android:id="@+id/NPColumns"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:minValue="3"
                app:maxValue="8" />
        </LinearLayout>

        <!-- Add binding button — gone by default, hanya BUTTON yang tampilkan ini.
             Default gone mencegah flash sebelum updateLayout() jalan pertama kali. -->
        <ImageButton
            android:id="@+id/btnAddBinding"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:src="@drawable/icon_add_24dp"
            app:tint="@color/colorPrimaryDark"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:visibility="gone" />

        <!-- Binding list container — gone by default, Java atur per type -->
        <LinearLayout
            android:id="@+id/LLBindings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone" />

        <!-- Slot icons (D_PAD, STICK, MENU_NAVIGATION) — gone by default -->
        <LinearLayout
            android:id="@+id/LLSlotIcons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            android:paddingTop="8dp"/>

        <!-- Legacy D_PAD binding spinners — tidak dipakai aktif,
             dipertahankan agar tidak breaking existing layout references -->
        <LinearLayout
            android:id="@+id/LLDPadBindings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/binding_up" />

            <Spinner
                style="@style/ComboBox"
                android:id="@+id/SBindingUp"
                android:layout_width="match_parent"
                android:spinnerMode="dialog" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/binding_right" />

            <Spinner
                style="@style/ComboBox"
                android:id="@+id/SBindingRight"
                android:layout_width="match_parent"
                android:spinnerMode="dialog" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/binding_down" />

            <Spinner
                style="@style/ComboBox"
                android:id="@+id/SBindingDown"
                android:layout_width="match_parent"
                android:spinnerMode="dialog" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/binding_left" />

            <Spinner
                style="@style/ComboBox"
                android:id="@+id/SBindingLeft"
                android:layout_width="match_parent"
                android:spinnerMode="dialog" />
        </LinearLayout>

        <!-- Toggle switch — gone by default, hanya BUTTON -->
        <CheckBox
            android:id="@+id/CBToggleSwitch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/toggle_switch"
            android:visibility="gone" />

        <!-- Custom text + icon picker — gone by default, Java atur visibility -->
        <LinearLayout
            android:id="@+id/LLCustomTextIcon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/custom_text" />

            <EditText
                style="@style/EditText"
                android:id="@+id/ETCustomText"
                android:maxLength="8"
                android:layout_width="match_parent"
                android:hint="@string/none"
                android:inputType="textCapCharacters" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/icon" />

            <HorizontalScrollView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp">

                <LinearLayout
                    android:id="@+id/LLIconList"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal" />
            </HorizontalScrollView>
        </LinearLayout>

        <!--
            MULTIPLE_BUTTON options — gone by default, tampil saat type = MULTIPLE_BUTTON.
            Seluruh UI sub-button dibangun secara dinamis oleh loadMultiButtonUI()
            di ControlsEditorActivity: jumlah tombol, arah per-button (8 arah + NONE/hidden),
            label text, dan combo binding per sub-button dirender sebagai section
            collapsible di dalam container ini.
        -->
        <LinearLayout
            android:id="@+id/LLMultiButtonOptions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            android:layout_marginTop="4dp"
            android:paddingTop="4dp"
            android:paddingBottom="8dp" />

    </LinearLayout>
</ScrollView>
